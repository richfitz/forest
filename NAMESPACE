import(Rcpp)
import(methods)
useDynLib(forest)

export(itree)

export(xnode)
export(xtree)

# TODO: rename both of these -- the first has a colision with
# phytools, the second presumes people only use this package.
export(read.newick)
export(write.newick)

# TODO: also rename
export(to.newick.string)
export(from.newick.string)
